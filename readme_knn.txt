The train_set_x, train_set_y, test_set_x csv files and the knn.py file for the code need to be in the same directory.

Libraries required prior to running the code:
sklearn --> for Tfidf only
pandas
numpy
string
re
math
collections

Code is now good to go!